{
  "name": "frontend-style-museum",
  "version": "1.0.0",
  "private": true,
  "description": "同一份内容的 30 种视觉风格与 30 种布局实验",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "check": "node scripts/validate.mjs",
    "build": "npm run generate && npm run check && node scripts/package.mjs",
    "dev": "node scripts/dev.mjs",
    "preview": "node scripts/serve.mjs dist 8082"
  },
  "engines": {
    "node": ">=20",
    "python": ">=3.10"
  }
}
